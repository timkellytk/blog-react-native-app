import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ShowScreen = () => {
  return (
    <View>
      <Text>Show screens</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ShowScreen;
